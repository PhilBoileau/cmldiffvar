% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmldiffvar.R
\name{cmldiffvar}
\alias{cmldiffvar}
\title{Causal Machine Learning for Differential Variance}
\usage{
cmldiffvar(
  data_tbl,
  estimand_type = "absolute",
  estimator_type = "tmle",
  confidence_level = 0.95,
  confounder_var_names,
  treatment_var_name,
  outcome_var_name,
  propensity_score_library = c("SL.mean", "SL.glm", "SL.earth"),
  cond_exp_outcome_library = c("SL.mean", "SL.glm", "SL.earth"),
  cond_exp_sq_outcome_library = c("SL.mean", "SL.glm", "SL.earth"),
  num_nuisance_sl_folds = 5,
  cross_fit = TRUE,
  num_cross_fit_folds = 5,
  parallel = FALSE
)
}
\arguments{
\item{data_tbl}{A \code{\link{data.frame}} or \code{\link[tibble:tibble]{tibble}}.}

\item{estimand_type}{A \code{character} indicating whether to estimate the
absolute (\code{"absolute"}) or relative (\code{"relative"}) differential variance.}

\item{estimator_type}{A \code{character} indicating whether to use a one-step
(\code{"one-step"}) or a targeted maximum likelihood estimator (\code{"tmle"}) in the
cross-fitting procedure.}

\item{confidence_level}{A \code{numeric} between $0.1$ and $0.99$ providing the
confidence level used to compute confidence intervals. Defaults to \code{0.95}.}

\item{confounder_var_names}{A \code{character} vector providing the columns names
of the treatment--outcome confounders stored in \code{data_tbl}.}

\item{treatment_var_name}{A \code{character} providing the column name of the
treatment assignment indicator stored in \code{data_tbl}.}

\item{outcome_var_name}{A \code{character} providing the column name of the
outcome variable stored in \code{data_tbl}.}

\item{propensity_score_library}{A \code{character} vector of candidate learners
used by the SuperLearner estimator of the propensity score. Defaults to
\code{c("SL.mean", "SL.glm", "SL.earth")}.}

\item{cond_exp_outcome_library}{A \code{character} vector of candidate learners
used by the SuperLearner estimator of the expected outcome conditional on
confounders and treatment assignment. Defaults to \code{c("SL.mean", "SL.glm", "SL.earth")}.}

\item{cond_exp_sq_outcome_library}{A \code{character} vector of candidate learners
used by the SuperLearner estimator of the expected squared outcome
conditional on confounders and treatment assignment. Defaults to
\code{c("SL.mean", "SL.glm", "SL.earth")}.}

\item{num_nuisance_sl_folds}{A \code{numeric} indicating the number of folds to
use in cross-validated SuperLearner estimators. Defaults to \code{5}.}

\item{cross_fit}{A \verb{logical flag} determining whether cross-fitted estimators
are used. Defaults to \code{TRUE}.}

\item{num_cross_fit_folds}{A \code{numeric} setting the number of folds to use by
the cross-fitting procedures, if used. Defaults to \code{5}.}

\item{parallel}{A \verb{logical flag} indicating whether the cross-fitting
procedure, if used, should be parallelized. Defaults to \code{FALSE}.}
}
\value{
A one-row \link[tibble:tibble]{tibble} containing the following columns:
\itemize{
\item \code{estimand}: The scale of the differential variance estimand
\item \code{estimator_type}: The type of estimator used
\item \code{confidence_level}: The pre-specified confidence level
\item \code{estimate}: The differential variance estimate
\item \code{se}: The estimator's standard error
\item \code{ci_low}: The lower bound of the Wald-type confidence interval
\item \code{ci_high}: The upper bound of the Wald-type confidence interval
\item \code{p_value}: The p-value of a two-sided test using the z-score of the
differential variance estimate
}
}
\description{
\code{cmldiffvar()} infers the differential variance between two
treatments using causal machine learning methods.
}
\details{
\code{cmldiffvar()} assumes that the data in \code{data_tbl} are generated
according to a parallel group study design with a binary treatment
variable, a continuous outcome, and continuous or binary treatment-outcome
confounders.

Under standard causal identifiability conditions --- namely, consistency,
positivity, and full exchangeability --- \code{cmldiffvar()} performs inference
on the differential variance of the potential outcomes. Differential
variance is defined on the absolute scale as the difference in potential
outcome variances of two treatments. On the relative scale, differential
variance is defined as the ratio of the potential outcome variances. The
scale of the differential variance estimated by \code{cmldiffvar()} is specified
by the \code{estimand_type} parameter.

These differential variance estimands rely on three nuisance parameters:
the propensity score, the expected outcome conditional on confounders and
treatment assignment, and the expected squared outcome condition on
confounders and treatment assignment.

\code{cmldiffvar()} implements cross-fitted one-step and targeted maximum
likelihood estimators of the differential variance estimands. These
cross-fitted estimators are used by default; non-cross-fitted estimators
can be used by setting \code{cross_fit = FALSE}. The number of folds used in
these cross-fitting procedures is set by \code{num_cross_fit_folds}. The
differential variance estimands' nuisance parameters are flexibly estimated
with \link[SuperLearner:SuperLearner]{SuperLearner} estimators.

The estimators implemented in \code{cmldiffvar()} are consistent if \emph{at least}
one of the following conditions is satisfied: (1) the propensity score is
consistently estimated, and  (2) the expected outcome conditional on confounders
and treatment assignment and the expected squared outcome conditional on
confounders and treatment assignment are consistently estimated.

The estimators implemented in \code{cmldiffvar()} are asymptotically linear ---
meaning their asymptotic sampling distribution is normally distributed
about the true differential variance parameter --- if \emph{all} nuisance
parameter estimators are consistently estimated at a rate of
\eqn{o_P(n^{-1/4})}. The confidence intervals, standard errors, and
p-values reported by \code{cmldiffvar()} are incorrect if these rate conditions
are not satisfied.

The cross-fitted estimation procedures can be parallelized by setting
\code{parallel = TRUE}. Parallelization relies on the \link[future:future]{future}
package. Instructions for setting up parallel processing are available in
the \link[future:future]{future} package's vignettes.
}
