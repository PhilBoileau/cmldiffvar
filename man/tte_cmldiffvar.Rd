% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tte-cmldiffvar.R
\name{tte_cmldiffvar}
\alias{tte_cmldiffvar}
\title{Causal Machine Learning for Differential Variance with Time-to-Event Outcomes}
\usage{
tte_cmldiffvar(
  data_tbl,
  restriction_time,
  homogeneity_type,
  homogeneous_effect_range_min,
  homogeneous_effect_range_max,
  propensity_score_adj_var_names,
  cond_event_haz_adj_var_names,
  cond_censoring_haz_adj_var_names,
  treatment_var_name,
  propensity_score_var_name = NULL,
  outcome_var_name,
  censoring_var_name,
  propensity_score_sl_library = c("SL.glm", "SL.earth", "SL.glmnet"),
  cond_event_haz_sl_library = c("SL.glm", "SL.earth", "SL.glmnet"),
  cond_censoring_haz_sl_library = c("SL.glm", "SL.earth", "SL.glmnet"),
  num_nuisance_sl_folds = 5
)
}
\arguments{
\item{data_tbl}{A \code{\link{data.frame}} or \code{\link[tibble:tibble]{tibble}}.}

\item{restriction_time}{A \code{numeric} integer corresponding to the
time-to-event outcomes restriction time.}

\item{homogeneity_type}{A \code{character} indicating the type of homogeneous
treatment effect under the null hypothesis: \code{"additive"} or
\code{"multiplicative"}.}

\item{homogeneous_effect_range_min}{A \code{numeric} providing the minimum
suspected possible effect size. The scaling of the effect size is dictated
by the \code{homogeneity_type} argument.}

\item{homogeneous_effect_range_max}{A \code{numeric} providing the maximum
suspected possible effect size. The scaling of the effect size is dictated
by the \code{homogeneity_type} argument.}

\item{propensity_score_adj_var_names}{A \code{character} vector providing the
columns names of the adjustment set variables for propensity score
estimation stored in \code{data_tbl}. Ignored if \code{propensity_score_var_name} is
\code{NULL}.}

\item{cond_event_haz_adj_var_names}{A \code{character} vector providing the
columns names of the adjustment set variables for conditional event hazard
rate estimation stored in \code{data_tbl}.}

\item{cond_censoring_haz_adj_var_names}{A \code{character} vector providing the
columns names of the adjustment set variables for conditional censoring
hazard rate estimation stored in \code{data_tbl}.}

\item{treatment_var_name}{A \code{character} providing the column name of the
treatment assignment indicator stored in \code{data_tbl}.}

\item{propensity_score_var_name}{An optional \code{character} providing the column
name of the treatment assignment indicator stored in \code{data_tbl}. Defaults
to \code{NULL}. See the Details section for more information.}

\item{outcome_var_name}{A \code{character} providing the column name of the
time-to-event outcome variable stored in \code{data_tbl}.}

\item{censoring_var_name}{A \code{character} providing the column name of the
censoring indicator stored in \code{data_tbl}.}

\item{cond_event_haz_sl_library}{A \code{character} vector of candidate learners
used by the SuperLearner estimator of the conditional event hazard rate.
Defaults to \code{c("SL.glm", "SL.earth", "SL.ranger")}.}

\item{cond_censoring_haz_sl_library}{A \code{character} vector of candidate learners
used by the SuperLearner estimator of the conditional event hazard rate.
Defaults to \code{c("SL.glm", "SL.earth", "SL.ranger")}.}

\item{num_nuisance_sl_folds}{A \code{numeric} indicating the number of folds to
use in cross-validated SuperLearner estimators. Defaults to \code{5}.}

\item{propensity_score_library}{A \code{character} vector of candidate learners
used by the SuperLearner estimator of the propensity score. Defaults to
\code{c("SL.glm", "SL.earth", "SL.ranger")}.}
}
\value{
A \code{list} containing two items:
\itemize{
\item \code{hypothesis_test_tbl}: A one-row \link[tibble:tibble]{tibble} reporting the
result of the homogeneous treatment effect hypothesis testing procedure.
\item \code{inference_tbl}: A \link[tibble:tibble]{tibble} reporting the differential
variance estimates and associated p-values considered for the homogeneous
treatment effect hypothesis testing procedure.
}
}
\description{
\code{tte_cmldiffvar()} performs a homogeneous treatment effect hypothesis testing
procedure based on the relative differential variance in datasets with
right-censored, restricted time-to-event outcomes. Two types of homogeneous
treatment hypotheses can be considered: a homogeneous additive effect, and a
homogeneous multiplicative effect.
}
