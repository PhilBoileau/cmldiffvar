% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unadjdiffvar.R
\name{unadjdiffvar}
\alias{unadjdiffvar}
\title{Unadjusted Differential Variance Inference}
\usage{
unadjdiffvar(
  data_tbl,
  estimand_type = "absolute",
  confidence_level = 0.95,
  treatment_var_name,
  propensity_score_var_name = NULL,
  outcome_var_name
)
}
\arguments{
\item{data_tbl}{A \code{\link{data.frame}} or \code{\link[tibble:tibble]{tibble}}.}

\item{estimand_type}{A \code{character} indicating whether to estimate the
absolute (\code{"absolute"}) or relative (\code{"relative"}) differential variance.}

\item{confidence_level}{A \code{numeric} between $0.1$ and $0.99$ providing the
confidence level used to compute confidence intervals. Defaults to \code{0.95}.}

\item{treatment_var_name}{A \code{character} providing the column name of the
treatment assignment indicator stored in \code{data_tbl}.}

\item{propensity_score_var_name}{An optional \code{character} providing the column
name of the treatment assignment indicator stored in \code{data_tbl}. Defaults
to \code{NULL}. See the Details section for more information.}

\item{outcome_var_name}{A \code{character} providing the column name of the
outcome variable stored in \code{data_tbl}.}
}
\value{
A one-row \link[tibble:tibble]{tibble} containing the following columns:
\itemize{
\item \code{estimand}: The scale of the differential variance estimand
\item \code{estimate}: The differential variance estimate
\item \code{se}: The estimator's standard error
\item \code{ci_low}: The lower bound of the Wald-type confidence interval
\item \code{ci_high}: The upper bound of the Wald-type confidence interval
\item \code{p_value}: The p-value of a two-sided test using the z-score of the
differential variance estimate
}
}
\description{
\code{unadjdiffvar()} infers the differential variance of two
treatments using an unadjusted estimator.
}
\details{
Under standard causal identifiability conditions --- namely,
consistency, positivity, and full exchangeability --- \code{unadjdiffvar()}
performs inference on the differential variance of the potential outcomes.
Differential variance is defined on the absolute scale as the difference in
potential outcome variances of two treatments. On the relative scale,
differential variance is defined as the ratio of the potential outcome
variances. The scale of the differential variance estimated by
\code{unadjdiffvar()} is specified by the \code{estimand_type} parameter.

\code{unadjdiffvar()} assumes that the data in \code{data_tbl} are generated
according to a parallel group study design with a binary treatment variable
and a continuous outcome. Treatment is assumed to be assigned at random. If
the treatment assignment probabilities are known, such as in a randomized
controlled trial, then these may be passed to the estimator using the
\code{propensity_score_var_name} argument. If there are (un)measured
confounders, this estimator will be biased.
}
